# Fuse Audit Report — 2026-04-17 13:05:22

Scope: retrospective audit of Waves W07 through W10.

Method:

- Read `docs/audit.md`, `docs/implementation-plan.md`, `STUBS.md`, `.claude/current-wave.json`, and `docs/implementation/wave07_concurrency_semantics.md` through `docs/implementation/wave10_pattern_matching.md`.
- Read `docs/learning-log.md` closure entries `WC007` through `WC010`.
- Read `tests/e2e/README.md`, `tests/e2e/spine_test.go`, `tests/e2e/match_test.go`, `compiler/lower/lower.go`, and `compiler/codegen/c11.go` to inspect the current proof and execution surfaces.
- Inspected wave commit triplets:
  - `21e96e7` / `ac7bc81` / `6e41bd8` — W07
  - `476e1ea` / `d927d2b` / `6313203` — W08
  - `30c7a62` / `6dea79a` / `a54c907` — W09
  - `da1bca7` / `3080a73` / `0760c94` — W10
- Executed the current verification surface locally, including the declared proof-of-completion commands for W07 through W10.

Local verification executed:

- `go test ./...`
- `go run tools/checkstubs/main.go -wave W10 -phase P00`
- `go run tools/checkstubs/main.go -wave W10`
- `go run tools/checkstubs/main.go -history-current-wave W10`
- `go run tools/checkgov/main.go -current-wave`
- W07 proof commands:
  - `go test ./compiler/check/... -run TestSendSyncMarkerTraits -v`
  - `go test ./compiler/check/... -run TestChannelTypecheck -v`
  - `go test ./compiler/check/... -run TestSpawnHandleTyping -v`
  - `go test ./compiler/check/... -run TestLockRankingEnforcement -v`
  - `go test ./tests/e2e/... -run TestConcurrencyRejections -v`
- W08 proof commands:
  - `go test ./compiler/monomorph/... -v`
  - `go test ./compiler/driver/... -run TestSpecializationInPipeline -v`
  - `go test ./tests/e2e/... -run TestIdentityGeneric -v`
  - `go test ./tests/e2e/... -run TestMultipleInstantiations -v`
- W09 proof commands:
  - `go test ./compiler/liveness/... -v`
  - `go test ./compiler/codegen/... -run TestDestructorCallEmitted -v`
  - `go test ./tests/e2e/... -run TestDropObservable -v`
  - `go test ./tests/e2e/... -run TestBorrowRejections -v`
- W10 proof commands:
  - `go test ./compiler/check/... -run TestExhaustivenessChecking -v`
  - `go test ./compiler/lower/... -run TestMatchDispatch -v`
  - `go test ./compiler/lower/... -run TestEnumDiscriminantAccess -v`
  - `go test ./compiler/lower/... -run TestOrRangePatterns -v`
  - `go test ./tests/e2e/... -run TestMatchEnumDispatch -v`

Manual W10 probes executed:

- Built `tests/e2e/match_enum_dispatch.fuse` through the real CLI and through `driver.Build` with `KeepC: true`.
- Inspected the generated C translation unit for `match_enum_dispatch.fuse`; it contains the expected discriminant comparisons and returns `42` for `Dir.North`.
- Launched the resulting binary with PowerShell `Start-Process -Wait -PassThru`; observed exit code `42`.
- Reproduced the failing Go launch path with a minimal `exec.Command` probe; on this Windows host it fails to start `match_enum_dispatch.exe` with `The requested operation requires elevation.`
- Ran the same Go launch probe against the W08 proof artifact `identity_generic.exe`; it exits `42` normally.

Limitations:

- Historical CI status for each closure SHA was not independently queried from GitHub in this workspace. The closure entries say CI was green, but checklist item A7 remains unverified by direct evidence here.
- This audit was performed on Windows. The W10 proof finding is therefore partly a host-specific launch finding, but it still matters because the repository's normal local proof command fails on this host.

Overall summary:

- W07: needs-rework
- W08: complete
- W09: needs-rework
- W10: needs-rework

Cross-wave findings:

1. Governance discipline is materially healthier from W07 onward than it was in the earlier waves. Each audited wave has a distinct `P00`, implementation, and `PCL` commit, and `WC007` through `WC010` now use the required five-field closure template.
2. W08 is the cleanest closure in this batch. The monomorphization package tests, the driver specialization test, and both source-level e2e proof programs all pass locally.
3. W07 and W09 both have honest closure notes, but the proof surfaces are still weaker than the strict audit bar because key user-visible behaviors are asserted through synthetic HIR or metadata-only tests rather than through real Fuse source compiled and exercised end-to-end.
4. W10's implementation surface looks substantially correct, but the official proof command is not reliable on this Windows host. The generated C and shell-launched executable behave correctly, while the Go `exec.Command` path used by the e2e harness fails specifically for `match_enum_dispatch.exe`.

## W07 — Concurrency Semantics

Closure SHA: `6e41bd8`

Checklist results:

- A — pass with limitation: the wave has distinct `P00`, implementation, and closure commits; `STUBS.md`, `.claude/current-wave.json`, and `WC007` align with the closure shape; CI on the closure SHA was not independently verified here.
- B — fail: the audit guide's proof-program bar is not met strictly. `tests/e2e/README.md` explicitly records W07 as a rejection proof with no `.fuse` source, and `TestConcurrencyRejections` asserts diagnostics over synthetic HIR construction rather than over real source programs.
- C — pass: the declared misuse cases are rejected loudly rather than silently accepted. The targeted checker tests and `TestConcurrencyRejections` all pass locally.
- D — fail with documented concession: `WC007` explicitly says negative impl syntax `impl !Trait for T {}` is not parsed yet and is only exposed through the checker API. That means part of the user-visible W07 promise is not source-real.
- E — fail: the wave does isolate the concurrency rules, but its externally testable surface is narrower than the wave document claims because negative impls and some rank/capture paths remain programmatic or synthetic.
- F — pass: no in-scope active-stub mismatch surfaced after the W07 stub retirement.
- G — pass with concern: the closure note is candid about the workaround, but the workaround is still real and should not be mistaken for a full end-to-end proof.

Red flags hit:

- No direct CI evidence was collected for the closure SHA in this workspace.
- The proof surface contains no real `.fuse` source program for the wave's user-visible rejection behavior.
- Negative impl syntax remains unparsed despite being named in the wave doc.

Verdict: needs-rework.

Smallest corrective action:

- Add real source-level rejection fixtures once parser/decorator plumbing can express them, and either implement negative impl syntax end-to-end or narrow the wave claim so it matches the actually shipped surface.

## W08 — Monomorphization

Closure SHA: `6313203`

Checklist results:

- A — pass with limitation: the three-commit discipline is followed, `WC008` uses the required template, and the wave's closure artifacts line up; CI on the closure SHA was not independently verified here.
- B — pass: `identity_generic.fuse` and `multiple_instantiations.fuse` both exist in `tests/e2e/`, are registered in `tests/e2e/README.md`, and pass locally through the real pipeline.
- C — pass: the monomorphization package tests show body duplication, deterministic specialized naming, call-site rewrite, partial-instantiation handling, and wrong-arity rejection rather than silent fallback.
- D — pass: the driver integration test `TestSpecializationInPipeline` passes, which is the strongest evidence that the wave is wired in the intended place rather than implemented as an isolated library exercise.
- E — pass: the wave remains dedicated to generic specialization and does not appear to be doing unrelated future-wave work.
- F — pass: no in-scope active-stub contract drift surfaced during this audit.
- G — pass: no workaround markers or placeholder shortcuts stood out in the implementation or proof surface.

Red flags hit:

- No direct CI evidence was collected for the closure SHA in this workspace.

Verdict: complete.

Smallest corrective action:

- Archive direct CI evidence for the closure SHA if the project wants future retrospective audits to satisfy checklist A7 without qualification.

## W09 — Ownership and Liveness

Closure SHA: `a54c907`

Checklist results:

- A — pass with limitation: the three-commit discipline is followed, the closure entry uses the required template, and the declared proof commands pass locally; CI on the closure SHA was not independently verified here.
- B — fail: the proof registry is honest, but it is not strong enough for a strict closure. `tests/e2e/README.md` records `drop_observable.fuse` as metadata-only, and it marks multiple W09 rejection cases as synthetic HIR assertions rather than source-driven failures.
- C — pass: the liveness/codegen checks fail loudly rather than silently accepting bad ownership behavior. The targeted liveness tests, `TestDestructorCallEmitted`, and `TestBorrowRejections` all pass locally.
- D — fail with concern: the wave plan calls for observable cleanup and borrow-rule enforcement, but `TestDropObservable` proves only that `DropIntent` metadata is produced, not that a real compiled Fuse program performs and exposes destructor behavior at runtime.
- E — pass with concern: W09 is still a dedicated ownership wave, but significant parts of the proof story are deferred behind grammar and spine limitations.
- F — pass: no in-scope active-stub mismatch surfaced after the W09 stub retirement.
- G — fail with concern: the synthetic HIR stand-ins for `ref`-typed fields, return borrows, use-after-move, and escaping borrow closures are explicit workaround markers. They are documented honestly, but they are still substitutes for end-to-end proofs.

Red flags hit:

- The most important W09 proof, `drop_observable.fuse`, is not an observable binary proof.
- Multiple rejection fixtures are recorded as synthetic HIR assertions rather than real source-level proofs.
- The closure note explicitly relies on grammar/spine gaps to justify those substitutions.

Verdict: needs-rework.

Smallest corrective action:

- Convert `drop_observable.fuse` into a true run-and-observe proof once the necessary runtime / multi-statement surface exists, and migrate the synthetic borrow cases to real compilable fixtures as the grammar and spine expand.

## W10 — Pattern Matching

Closure SHA: `0760c94`

Checklist results:

- A — pass with limitation: the wave has distinct `P00`, implementation, and closure commits; `WC010` uses the required template; and the W10 stub/governance checks pass locally. CI on the closure SHA was not independently verified here.
- B — fail: the official proof command `go test ./tests/e2e/... -run TestMatchEnumDispatch -v` fails locally, and therefore `go test ./...` fails locally as well.
- C — pass: the checker and lowerer proof commands pass locally, and the generated C for `match_enum_dispatch.fuse` shows the expected multi-block dispatch shape with discriminant comparisons against `0` and `1`.
- D — pass with risk: the implementation appears semantically correct in the current tree. The built binary exits `42` when launched through `Start-Process`, and the emitted C returns `42` for `Dir.North`. The observed failure is concentrated in the proof harness's Go process-launch path on Windows.
- E — pass: the wave remains focused on pattern-checking and dispatch.
- F — pass: no in-scope active-stub mismatch surfaced after the W10 stub retirement.
- G — fail with risk: `tests/e2e/spine_test.go` uses `exec.Command(binPath)` and ignores launch errors. On this Windows host, a minimal Go probe reproduces a launch failure for `match_enum_dispatch.exe` with `The requested operation requires elevation.`, while the W08 proof artifact `identity_generic.exe` launches through the same Go path and exits `42`. That makes the W10 proof command host-fragile even if the generated program itself is correct.

Red flags hit:

- The repository's standard local proof command for W10 fails.
- The proof harness path and the shell-launch path disagree about the same generated artifact on Windows.
- The current e2e helper ignores launch errors and therefore collapses distinct failure modes into an exit-code assertion.

Verdict: needs-rework.

Smallest corrective action:

- Fix the Windows e2e launch path so `TestMatchEnumDispatch` is green in the standard Go harness, and teach the helper to surface process-launch errors explicitly instead of returning only `ProcessState.ExitCode()`.