# Fuse Audit Report — 2026-04-17 10:56:56

Supersedes: `docs/audit/report-20260417-103625.md`.

Scope: retrospective audit of Waves W00 through W06.

Method:

- Read `docs/audit.md`, `docs/implementation-plan.md`, `STUBS.md`, `.claude/current-wave.json`, and `docs/implementation/wave00_governance.md` through `docs/implementation/wave06_type_checking.md`.
- Read `docs/learning-log.md` closure entries `WC000` through `WC006`.
- Inspected wave closure / implementation commits:
  - `d609313` — W00
  - `962d41c` — W01
  - `ca88b11` — W02
  - `f132793` — W03
  - `7624fab` — W04
  - `9eac416` — W05
  - `35a4204` — W06
- Executed current verification surface locally.

Local verification executed successfully:

- `go test ./...`
- `go run tools/checkstubs/main.go`
- `go run tools/checkstubs/main.go -history-current-wave W06`
- `go run tools/checkstubs/main.go -wave W06 -phase P00`
- `go run tools/checkstubs/main.go -wave W06`
- `go run tools/checkgov/main.go -current-wave`
- `go test ./tests/e2e/... -run TestCheckerBasicProof -v`
- `make all`
- `make test`
- `test -f docs/phase-model.md`
- `go run tools/checkdocs/main.go`

Manual end-to-end probes executed:

- Built and ran a W06-style multi-function program through `go run ./cmd/fuse build ...`; produced a native binary and exited `42`.
- Probed current active-stub surfaces and captured emitted diagnostics.

Limitations:

- Historical CI status for each closure SHA was not independently queried from GitHub in this workspace. Commit messages and closure entries claim CI green, but checklist item A7 remains unverified by direct evidence here.
- This audit is retrospective and is being performed in the same broad session lineage as prior repo inspection. That is itself one of the audit guide’s explicit red flags and should be treated as a process smell, not as proof of correctness.

Overall summary:

- W00: needs-rework
- W01: needs-rework
- W02: needs-rework
- W03: needs-rework
- W04: needs-rework
- W05: needs-rework
- W06: needs-rework

Cross-wave findings:

1. The repository is materially healthier than the prior report could prove. The full Go suite is green locally, governance tools pass, and the W06 end-to-end proof path works through the real CLI.
2. The closure-commit hygiene problem remains unchanged for W00 through W06: each wave’s implementation, `STUBS.md` update, `.claude/current-wave.json`, and wave closure entry land in the same commit.
3. No distinct pre-implementation `P00 Stub Audit` commit was found for W00 through W06.
4. `WC003`, `WC004`, `WC005`, and `WC006` do not use the required five-field closure-entry template defined in `docs/learning-log.md`.
5. The live `STUBS.md` Active table still does not match actual emitted diagnostics for multiple active rows. This is now directly confirmed by executing the compiler, not just by reading code.
6. The old W00 portability concern is downgraded: the wave’s shell-based verification commands do succeed in this Windows environment. It remains a portability risk because they rely on external `make` / `test.exe` tooling rather than purely project-owned or Go-native commands.

Concrete active-stub mismatch evidence:

- Pattern matching row declares `"pattern matching not yet implemented"`, but a `match` probe currently emits `"spine does not yet lower *hir.MatchExpr"` from the lowerer.
- Concurrency checker row declares `"concurrency checker not yet implemented"`, but a `spawn` probe currently emits `"spine does not yet lower *hir.SpawnExpr"`.
- Closures row declares `"closures not yet implemented"`, but a helper function returning a closure currently emits `"spine does not yet lower *hir.ClosureExpr"`.
- CLI row declares `"subcommand not yet implemented"`, but `go run ./cmd/fuse frobnicate` emits `fuse: unknown subcommand "frobnicate" (W05 CLI provides version, help, build)`.

## W00 — Governance and Phase Model

Closure SHA: `d609313`

Checklist results:

- A — fail: closure artifacts exist and the wave’s commands now run locally, but there is no distinct pre-implementation `P00` commit, the closure commit is also the implementation commit, and CI on the closure SHA was not independently verified here.
- B — N/A: W00 introduces no user-visible language behavior and predates the W05 proof-program requirement.
- C — N/A: silent-stub implementation traps are not in scope for W00.
- D — N/A: W00 is governance and scaffolding, not a syntax / semantic / backend wave.
- E — N/A: no cross-cutting language feature is introduced.
- F — pass: governance artifacts and validation tools are present and locally runnable.
- G — pass with risk: `make all`, `make test`, `test -f docs/phase-model.md`, and `go run tools/checkdocs/main.go` all succeed here, but the wave still depends on non-Go shell tools that are not obviously guaranteed on every Windows host.

Red flags hit:

- Closure commit and implementation commit are the same commit: `d609313`.
- No distinct `P00 Stub Audit` commit was found before implementation.

Verdict: needs-rework.

Smallest corrective action:

- Add a retrospective governance note recording that W00 lacked a separate `P00` and closure commit, and revise the W00 plan language to prefer project-owned / Go-native verification commands.

## W01 — Lexer

Closure SHA: `962d41c`

Checklist results:

- A — fail: the implementation, `STUBS.md` update, `WC001`, and `.claude/current-wave.json` all land in the same commit; no distinct `P00` commit was found; CI on the closure SHA was not independently verified here.
- B — N/A: W01 predates the W05 end-to-end proof-program requirement.
- C — N/A: later-wave silent-stub checks do not apply.
- D — pass: the lexer explicitly closes the L001 ambiguity contracts for BOM rejection, raw strings, and `?.` tokenization.
- E — N/A: W01 is not a cross-cutting feature wave under checklist E.
- F — pass: lexical diagnostics carry span, one-line message, and hint.
- G — pass: no meaningful workaround or silent-default pattern specific to the lexer surfaced.

Red flags hit:

- Closure commit and implementation commit are the same commit: `962d41c`.
- No distinct `P00 Stub Audit` commit was found before implementation.

Verdict: needs-rework.

Smallest corrective action:

- Record the missing `P00` / closure-commit separation in a retrospective governance note and enforce separation from future waves onward.

## W02 — Parser and AST

Closure SHA: `ca88b11`

Checklist results:

- A — fail: the parser / AST implementation, `STUBS.md`, `WC002`, and `.claude/current-wave.json` land together; no distinct `P00` commit was found; CI on the closure SHA was not independently verified here.
- B — N/A: W02 predates the W05 proof-program requirement.
- C — N/A: later-wave silent-stub traps are not yet in scope.
- D — pass: the parser remains syntax-only and addresses the ambiguity-control rules from L001.
- E — N/A: W02 is not a cross-cutting feature wave in the checklist-E sense.
- F — pass: malformed-input behavior is defensive and diagnostic-driven rather than panic-driven.
- G — pass: no meaningful workaround markers or silent-default parser shortcuts surfaced.

Red flags hit:

- Closure commit and implementation commit are the same commit: `ca88b11`.
- No distinct `P00 Stub Audit` commit was found before implementation.

Verdict: needs-rework.

Smallest corrective action:

- Record the missing `P00` / closure-commit separation and enforce it going forward.

## W03 — Resolution

Closure SHA: `f132793`

Checklist results:

- A — fail: no distinct `P00` commit was found before implementation, the closure SHA is also the implementation SHA, `WC003` does not use the required closure template, and CI on the closure SHA was not independently verified here.
- B — N/A: W03 predates the W05 proof-program requirement.
- C — N/A: later-wave silent-stub checks are not yet in scope.
- D — pass: the resolver respects deterministic discovery, `@cfg`-before-downstream filtering, and explicit visibility enforcement.
- E — N/A: W03 is not a dedicated cross-cutting feature wave under checklist E.
- F — pass: the resolution surface is diagnostic-driven and not obviously fallback-driven.
- G — pass: no concrete workaround markers or silent-default placeholders specific to W03 surfaced.

Red flags hit:

- Closure commit and implementation commit are the same commit: `f132793`.
- No distinct `P00 Stub Audit` commit was found before implementation.
- `WC003` is narrative-only and omits the required fields `Proof programs added this wave`, `Stubs retired this wave`, `Stubs introduced this wave`, `What was harder than planned`, and `What the next wave must know`.

Verdict: needs-rework.

Smallest corrective action:

- Rewrite `WC003` into the mandated template and add a retrospective governance note about the missing `P00` / closure-commit separation.

## W04 — HIR and TypeTable

Closure SHA: `7624fab`

Checklist results:

- A — fail: no distinct `P00` commit was found before implementation, the closure SHA is also the implementation SHA, `WC004` does not use the required closure template, and CI on the closure SHA was not independently verified here.
- B — N/A: W04 predates the W05 proof-program requirement.
- C — N/A: later-wave silent-stub checks are not yet in scope.
- D — pass: W04 still strongly satisfies the intended architecture invariants, especially no `Unknown` fallback, structured patterns, stable identity, and deterministic pass ordering.
- E — pass: the pass-graph and incremental foundation remain a dedicated architectural concern rather than a retrofitted afterthought.
- F — pass: invariants are explicit and the failure modes are structural rather than silent.
- G — pass: no meaningful workaround or quiet-default pattern specific to W04 surfaced.

Red flags hit:

- Closure commit and implementation commit are the same commit: `7624fab`.
- No distinct `P00 Stub Audit` commit was found before implementation.
- `WC004` is narrative-only and omits the required closure-template fields.

Verdict: needs-rework.

Smallest corrective action:

- Rewrite `WC004` into the required template and add a retrospective governance correction for the missing `P00` / closure-commit separation.

## W05 — Minimal End-to-End Spine

Closure SHA: `9eac416`

Checklist results:

- A — fail: no distinct `P00` commit was found before implementation, the closure SHA is also the implementation SHA, `WC005` does not use the required closure template, and active-stub reality no longer fully matches `STUBS.md`.
- B — pass with concern: W05 proof programs are present, registered, and green locally; however `hello_exit.fuse` remains too ceremonial to be a strong adversarial proof on its own.
- C — N/A: later-wave silent-stub implementation checks are not yet in scope.
- D — pass: the W05 spine still fails loudly rather than silently approximating unsupported semantics.
- E — pass: W05 remains intentionally narrow instead of smuggling in broader semantics.
- F — fail: active stub rows still do not reliably emit the exact diagnostics declared in `STUBS.md`.
- G — pass: no evidence of silent defaults or placeholder codegen shortcuts specific to W05 surfaced in the current code.

Red flags hit:

- Closure commit and implementation commit are the same commit: `9eac416`.
- No distinct `P00 Stub Audit` commit was found before implementation.
- `WC005` is narrative-only and omits the required closure-template fields.
- `hello_exit.fuse` is still a ceremonial proof relative to checklist B3.

Verdict: needs-rework.

Smallest corrective action:

- Rewrite `WC005` into the mandated template, and either strengthen `hello_exit.fuse` or treat `exit_with_value.fuse` as the real adversarial proof while documenting the limitation.

## W06 — Type Checking

Closure SHA: `35a4204`

Checklist results:

- A — fail: the full suite is green locally, `checkstubs` and `checkgov` pass, and the W06 retirement checks pass, but no distinct `P00` commit was found before implementation, the closure SHA is also the implementation SHA, `WC006` does not use the required closure template, and active-stub reality does not fully match `STUBS.md`.
- B — pass: `checker_basic.fuse` exists, is registered in `tests/e2e/README.md`, passes locally, and is materially more adversarial than the W05 `hello_exit` proof because it requires typed parameters, a secondary function, a direct call, and consistent typing through the full pipeline.
- C — N/A: the prior-attempt-specific silent-stub checks that activate at W07+ / W08+ / W09+ / W10+ / W11+ / W12+ / W13+ are not yet fully in scope.
- D — pass: the checker wave upholds the key architectural invariants relevant to W06, especially the no-`KindInfer`-after-check contract and the L002 defense against skipping stdlib bodies.
- E — pass with concern: W06 is correctly its own dedicated wave, but its proof surface is still concentrated in one end-to-end program plus unit coverage rather than multiple e2e proofs for the broader checker feature set.
- F — fail: checklist F2 fails directly in the current wave packages. A `match` probe and a `spawn` probe do not emit the declared active-stub texts from `STUBS.md`; they emit generic lowerer diagnostics instead.
- G — pass: no concrete evidence of silent defaulting or placeholder lowering was found inside the implemented W06 checker itself; the main issue is stub-contract drift between registry and emitted diagnostics.

Red flags hit:

- Closure commit and implementation commit are the same commit: `35a4204`.
- No distinct `P00 Stub Audit` commit was found before implementation.
- `WC006` is narrative-only and omits the required closure-template fields.

Concrete W06 findings:

1. W06 materially works.
   - `go test ./compiler/check/...` passes as part of the full suite.
   - `go test ./tests/e2e/... -run TestCheckerBasicProof -v` passes.
   - A manual CLI probe compiling a multi-function program produced a native binary that exited `42`.

2. The current-wave active stub table still fails the audit guide’s exact-diagnostic requirement.
   - `match` probe result: `spine does not yet lower *hir.MatchExpr`
   - `spawn` probe result: `spine does not yet lower *hir.SpawnExpr`
   - Both differ from the declared `STUBS.md` texts for pattern matching and concurrency.

3. W06 still inherits the closure-process hygiene defect.
   - The implementation, closure entry, `STUBS.md` update, and `.claude/current-wave.json` update all land in `35a4204`.

Verdict: needs-rework.

Smallest corrective action:

- Make the active `STUBS.md` rows for current-wave-visible stub behavior match the real emitted diagnostics, or change the compiler to emit the declared exact texts. Then rewrite `WC006` into the required template.

## Most Important Repository-Level Actions

1. Enforce separate commits for `P00 Stub Audit`, implementation, and `PCL` closure from W07 onward.
2. Rewrite `WC003` through `WC006` into the required five-field closure template.
3. Fix the live `STUBS.md` / emitted-diagnostic drift. This is now directly reproduced for `MatchExpr`, `SpawnExpr`, `ClosureExpr`, and unknown CLI subcommands.
4. Keep the stronger W06-style proof approach. `checker_basic.fuse` is a better template for future waves than the ceremonial `hello_exit.fuse`.
